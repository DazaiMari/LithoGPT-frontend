version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://103.143.81.103:8091
    container_name: lithogpt-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - lithogpt-network

networks:
  lithogpt-network:
    driver: bridge
